{
  "name": "@ax/bridge",
  "version": "0.1.0",
  "type": "module",
  "description": "Bridge between Chrome DevTools Protocol and WebSocket server",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.pkg.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ax/core": "workspace:*",
    "ws": "^8.16.0",
    "chrome-remote-interface": "^0.33.0",
    "jsondiffpatch": "^0.6.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.3.0",
    "@types/node": "latest",
    "@types/ws": "^8.5.0",
    "msw": "^2.0.0"
  }
}