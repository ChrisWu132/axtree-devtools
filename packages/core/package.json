{
  "name": "@ax/core",
  "version": "0.1.0",
  "type": "module",
  "description": "Core accessibility tree parsing and manipulation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.pkg.json",
    "test": "vitest",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "jsondiffpatch": "^0.6.2"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "^5.4.0",
    "vitest": "^1.3.0"
  }
}